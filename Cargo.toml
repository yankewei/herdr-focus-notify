[package]
name = "herdr-focus-notify"
version = "0.3.3"
edition = "2021"
license = "MIT"
description = "Clickable macOS desktop notifications that focus the relevant Herdr agent pane."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
